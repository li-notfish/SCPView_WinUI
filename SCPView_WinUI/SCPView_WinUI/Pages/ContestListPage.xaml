<Page
    x:Class="SCPView_WinUI.Pages.ContestListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:model="using:SCPView_WinUI.Data.Model"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding ContestListPage, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ScrollView Grid.Row="1">
            <ListView
                x:Name="ContestListView"
                IsItemClickEnabled="True"
                ItemsSource="{Binding ContestItems}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:SCPContestItem">
                        <Grid Padding="8,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                FontSize="16"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="{Binding Title}"
                                TextWrapping="Wrap" />
                            <TextBlock
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Text="{Binding Author}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>

        <Grid Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="backButton"
                Margin="39,59,39,0"
                VerticalAlignment="Top"
                AutomationProperties.AutomationId="BackButton"
                AutomationProperties.ItemType="Navigation Button"
                AutomationProperties.Name="Back"
                Command="{Binding GoBackPageCommand}"
                Style="{StaticResource NavigationBackButtonNormalStyle}" />
            <TextBlock
                x:Name="pageTitle"
                Grid.Column="1"
                Margin="0,0,30,40"
                VerticalAlignment="Bottom"
                FontWeight="ExtraBold"
                Foreground="White"
                IsHitTestVisible="False"
                Style="{StaticResource HeaderTextBlockStyle}"
                Text="{Binding ContestTitle}"
                TextWrapping="NoWrap" />
            <ProgressBar
                Grid.Column="1"
                VerticalAlignment="Bottom"
                HorizontalContentAlignment="Stretch"
                IsIndeterminate="True"
                ShowError="False"
                ShowPaused="False"
                Visibility="{Binding ProcessBarVisibility}" />
        </Grid>
    </Grid>
</Page>
